## Append to array
```javascript
var monTableau = [];
monTableau.push['item1'];
```

## Test if contains
```javascript
 array.indexOf(item) != -1
 ```

## Test if contains (JQuery)
```javascript
 $.inArray(item, array)) != -1
 ```
